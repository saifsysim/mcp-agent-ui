# MCP Agent UI — Environment Config
# Copy to .env and fill in values.

# URL of the mcp-github-agent backend
MCP_BACKEND_URL=http://localhost:8000

# Port this UI server listens on (default: 8001)
PORT=8001
